import random
def genera():
      l=range[1,42]
      lista=[]
      while len(lista)<6:
            numero=random.choice(L)
            if not(numero)in lista:
                  lista.append(numero)
return lista
